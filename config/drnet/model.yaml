association:
    embedding_dim: 32
    hidden_dim: [64, 32, 16]
    beta: 0.5
    dropout: 0.2
    agg: cat

affection:
    embedding_dim: 64
    hidden_dim: [128, 64, 32]
    dropout: 0.2
    agg: cat